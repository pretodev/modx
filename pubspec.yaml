name: modulex
description: A new Flutter package project.
version: 0.0.1
homepage: https://github.com/pretodev/modulex

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.5.1
  fpdart: ^0.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
